services:
  - type: web
    name: rachita-uduppu
    env: node
    plan: free
    buildCommand: npm run render-build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_PASSWORD
        value: Arthi123!@#
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
